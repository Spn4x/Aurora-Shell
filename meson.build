# aurora-shell/meson.build

project('aurora-shell', 'c',
  version: '0.1.0',
  default_options: [
    'c_std=c11',
    'warning_level=2',
  ]
)

# Process the build files in our subdirectories
# Note the paths match YOUR directory structure now
subdir('src/aurora-shell')
subdir('widgets')